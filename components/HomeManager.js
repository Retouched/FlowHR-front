import styles from "../styles/HomeManager.module.css";

function HomeManager() {
  return (
    <div>
      <main className={styles.main}>
        <h1 className={styles.title}>HomeManager</h1>
      </main>
    </div>
  );
}

export default HomeManager;
